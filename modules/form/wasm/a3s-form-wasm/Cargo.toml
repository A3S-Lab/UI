[package]
name = "a3s-form-wasm"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Allocation-free SHA-256 accelerator for A3S Form"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
