# v0.1.0 矩阵字段

`matrix-single` 和 `matrix-multiple` 不属于 v0.1.0 稳定契约。v0.1.0 文档、编译能力和运行时组件保持不变。

矩阵字段当前位于 `next` 开发版，包含显式行列类型、Schema 对齐校验、Designer 配置、响应式运行时和行级错误定位。

[查看开发版矩阵字段文档](/next/guide/matrix-fields)
