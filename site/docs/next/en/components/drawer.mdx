# Drawer

Drawer presents a supporting task or detail from the page edge, preserving context while providing a larger temporary workspace.

<Preview>
<button type="button" class="btn" data-variant="outline" onclick="document.getElementById('demo-drawer').showModal()">Open Drawer</button>
<dialog id="demo-drawer" class="drawer" aria-labelledby="demo-drawer-title" aria-describedby="demo-drawer-description">
  <article>
    <header class="mx-auto w-full max-w-sm">
      <h2 id="demo-drawer-title">Adjust daily goal</h2>
      <p id="demo-drawer-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <div class="mx-auto w-full max-w-sm">
        <div class="flex items-center justify-center gap-6">
          <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="Decrease"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14" /></svg></button>
          <div class="text-center">
            <div class="text-7xl font-bold">350</div>
            <div class="text-muted-foreground text-xs uppercase">Calories/day</div>
          </div>
          <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="Increase"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14" /><path d="M12 5v14" /></svg></button>
        </div>
      </div>
    </section>
    <footer class="mx-auto w-full max-w-sm">
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
</Preview>

## Usage

<Steps>
  <Step title="Include CSS">

Import Tailwind and one full A3S UI style bundle.

```css
@import "tailwindcss";
@import "@a3s-lab/ui/a3s.css";
```

Or import only the base CSS, Drawer component CSS, and one style pack.

```css
@import "tailwindcss";
@import "@a3s-lab/ui/base.css";
@import "@a3s-lab/ui/components/drawer.css";
@import "@a3s-lab/ui/styles/a3s.css";
```

Using CDN or bundler imports? See the [Installation page](/installation).

  </Step>
  <Step title="Include JavaScript">

Copy or serve the full A3S UI JavaScript bundle.

```html
<script src="/assets/js/all.min.js" defer></script>
```

Or copy or serve the A3S UI runtime and Drawer script.

```html
<script src="/assets/js/runtime.min.js" defer></script>
<script src="/assets/js/drawer.min.js" defer></script>
```

Using CDN or bundler imports? See the [Installation page](/installation).

  </Step>
  <Step title="Add your drawer HTML">

A3S UI maps shadcn/ui's Vaul Drawer to a native `<dialog>` element. This keeps modality, focus handling, and inert page content in the browser platform. The Drawer script adds safe backdrop-click close and lets the exit animation finish before calling the native close. Drag gestures, snap points, and the drag handle are intentionally not included.

```html
<button type="button" class="btn" data-variant="outline" onclick="document.getElementById('demo-drawer').showModal()">Open Drawer</button>
<dialog id="demo-drawer" class="drawer" aria-labelledby="demo-drawer-title" aria-describedby="demo-drawer-description">
  <article>
    <header class="mx-auto w-full max-w-sm">
      <h2 id="demo-drawer-title">Adjust daily goal</h2>
      <p id="demo-drawer-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <div class="mx-auto w-full max-w-sm">
        <div class="flex items-center justify-center gap-6">
          <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="Decrease">...</button>
          <div class="text-center">
            <div class="text-7xl font-bold">350</div>
            <div class="text-muted-foreground text-xs uppercase">Calories/day</div>
          </div>
          <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="Increase">...</button>
        </div>
      </div>
    </section>
    <footer class="mx-auto w-full max-w-sm">
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
```

  </Step>
</Steps>

### HTML structure

<Callout type="warning" title="No drag handle" icon="triangle-alert">
  A3S UI does not implement Vaul's drag-to-close gesture, snap points, or drag handle. Drawers close with outside click, Escape, or explicit close controls.
</Callout>

<dl>
  <dt><code>&lt;button type="button" onclick="drawer.showModal()"&gt;</code> <span class="badge" data-variant="secondary">Optional</span></dt>
  <dd>Trigger button. A3S UI intentionally uses the native <code>HTMLDialogElement.showModal()</code> method.</dd>
  <dt><code>&lt;dialog class="drawer" data-side="bottom|top|left|right"&gt;</code></dt>
  <dd>
    Native modal drawer. Omit <code>data-side</code> for the default bottom drawer. Add <code>aria-labelledby</code> and <code>aria-describedby</code> when title and description are present. JavaScript adds the <code>data-drawer-initialized</code> flag and manages animated close behavior.
    <dl>
      <dt><code>&lt;article&gt;</code></dt>
      <dd>
        Drawer content surface.
        <dl>
          <dt><code>&lt;header&gt;</code> <span class="badge" data-variant="secondary">Optional</span></dt>
          <dd>Drawer header with a title and optional description.</dd>
          <dt><code>&lt;section&gt;</code> <span class="badge" data-variant="secondary">Optional</span></dt>
          <dd>Scrollable body/content area.</dd>
          <dt><code>&lt;footer&gt;</code> <span class="badge" data-variant="secondary">Optional</span></dt>
          <dd>Action area pinned after the drawer body.</dd>
        </dl>
      </dd>
    </dl>
  </dd>
</dl>

### JavaScript API

React `useDrawer` and the equivalent Vue composable expose native `showModal()` and the enhanced `close(returnValue)`. `close()` waits for an exit transition no longer than 220ms and completes immediately under reduced motion.

## Examples

### Sides

Use `data-side` to set the viewport side. Available values are `top`, `right`, `bottom`, and `left`.

<Preview>
<div class="flex flex-wrap gap-2">
  <button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-side-top').showModal()">Top</button>
  <button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-side-right').showModal()">Right</button>
  <button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-side-bottom').showModal()">Bottom</button>
  <button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-side-left').showModal()">Left</button>
</div>
<dialog id="drawer-side-top" class="drawer" data-side="top" aria-labelledby="drawer-side-top-title" aria-describedby="drawer-side-top-description">
  <article class="max-h-[50vh]">
    <header>
      <h2 id="drawer-side-top-title">Adjust daily goal</h2>
      <p id="drawer-side-top-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <dl class="grid gap-4 py-1">
        <div><dt class="font-medium">Completed tasks</dt><dd class="text-sm text-muted-foreground">8 per day</dd></div>
        <div><dt class="font-medium">Focus time</dt><dd class="text-sm text-muted-foreground">90 minutes</dd></div>
        <div><dt class="font-medium">Reminder</dt><dd class="text-sm text-muted-foreground">Weekdays at 5:30 PM</dd></div>
      </dl>
    </section>
    <footer>
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
<dialog id="drawer-side-right" class="drawer" data-side="right" aria-labelledby="drawer-side-right-title" aria-describedby="drawer-side-right-description">
  <article>
    <header>
      <h2 id="drawer-side-right-title">Adjust daily goal</h2>
      <p id="drawer-side-right-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <dl class="grid gap-4 py-1">
        <div><dt class="font-medium">This week</dt><dd class="text-sm text-muted-foreground">31 of 40 tasks completed</dd></div>
        <div><dt class="font-medium">Current streak</dt><dd class="text-sm text-muted-foreground">4 workdays</dd></div>
        <div><dt class="font-medium">Recommendation</dt><dd class="text-sm text-muted-foreground">Keep the target at 8 based on the last two weeks.</dd></div>
      </dl>
    </section>
    <footer>
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
<dialog id="drawer-side-bottom" class="drawer" data-side="bottom" aria-labelledby="drawer-side-bottom-title" aria-describedby="drawer-side-bottom-description">
  <article class="max-h-[50vh]">
    <header>
      <h2 id="drawer-side-bottom-title">Adjust daily goal</h2>
      <p id="drawer-side-bottom-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <dl class="grid gap-4 py-1 sm:grid-cols-3">
        <div><dt class="font-medium">Task target</dt><dd class="text-sm text-muted-foreground">8 per day</dd></div>
        <div><dt class="font-medium">Focus target</dt><dd class="text-sm text-muted-foreground">90 minutes per day</dd></div>
        <div><dt class="font-medium">Rest days</dt><dd class="text-sm text-muted-foreground">Saturday and Sunday</dd></div>
      </dl>
    </section>
    <footer>
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
<dialog id="drawer-side-left" class="drawer" data-side="left" aria-labelledby="drawer-side-left-title" aria-describedby="drawer-side-left-description">
  <article>
    <header>
      <h2 id="drawer-side-left-title">Adjust daily goal</h2>
      <p id="drawer-side-left-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <dl class="grid gap-4 py-1">
        <div><dt class="font-medium">Counting rule</dt><dd class="text-sm text-muted-foreground">Only completed and accepted tasks count.</dd></div>
        <div><dt class="font-medium">Carry-over</dt><dd class="text-sm text-muted-foreground">Unfinished tasks do not count toward the next day.</dd></div>
        <div><dt class="font-medium">Effective date</dt><dd class="text-sm text-muted-foreground">Saving updates today's progress immediately.</dd></div>
      </dl>
    </section>
    <footer>
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
</Preview>

### Scrollable content

Keep actions visible while the drawer body scrolls.

<Preview>
<button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-scrollable').showModal()">Scrollable Content</button>
<dialog id="drawer-scrollable" class="drawer" data-side="right" aria-labelledby="drawer-scrollable-title" aria-describedby="drawer-scrollable-description">
  <article>
    <header>
      <h2 id="drawer-scrollable-title">Adjust daily goal</h2>
      <p id="drawer-scrollable-description">Set your daily activity goal.</p>
    </header>
    <section class="px-4">
      <div class="grid gap-6 py-1">
        <div><h3 class="mb-1 font-medium">Task target</h3><p class="text-sm leading-normal text-muted-foreground">Complete 8 accepted tasks each day. Drafts, canceled tasks, and failed automations do not count.</p></div>
        <div><h3 class="mb-1 font-medium">Focus target</h3><p class="text-sm leading-normal text-muted-foreground">Accumulate 90 focused minutes. Sessions shorter than five minutes are ignored to avoid accidental tracking.</p></div>
        <div><h3 class="mb-1 font-medium">Workdays</h3><p class="text-sm leading-normal text-muted-foreground">Goals apply Monday through Friday. Holidays and manually marked rest days preserve the current streak.</p></div>
        <div><h3 class="mb-1 font-medium">Reminder</h3><p class="text-sm leading-normal text-muted-foreground">Send one reminder at 5:30 PM on workdays, unless the goal is already complete or today is a rest day.</p></div>
        <div><h3 class="mb-1 font-medium">Scope</h3><p class="text-sm leading-normal text-muted-foreground">Personal tasks and current team projects count. Historical tasks from archived projects are not recalculated.</p></div>
        <div><h3 class="mb-1 font-medium">Effective date</h3><p class="text-sm leading-normal text-muted-foreground">Saving updates today's progress immediately while preserving previous days for before-and-after comparisons.</p></div>
      </div>
    </section>
    <footer>
      <button type="button" class="btn" onclick="this.closest('dialog').close()">Submit</button>
      <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">Cancel</button>
    </footer>
  </article>
</dialog>
</Preview>

### RTL

Drawer sides are physical viewport sides. Content spacing and text alignment follow document direction.

<Preview>
<div dir="rtl">
  <button type="button" class="btn" data-variant="outline" onclick="document.getElementById('drawer-rtl').showModal()">فتح الدرج</button>
  <dialog id="drawer-rtl" class="drawer" aria-labelledby="drawer-rtl-title" aria-describedby="drawer-rtl-description">
    <article>
      <header class="mx-auto w-full max-w-sm">
        <h2 id="drawer-rtl-title">نقل الهدف</h2>
        <p id="drawer-rtl-description">حدد هدف نشاطك اليومي.</p>
      </header>
      <section class="px-4">
        <div class="mx-auto w-full max-w-sm">
          <div class="flex items-center justify-center gap-6">
            <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="تقليل"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14" /></svg></button>
            <div class="text-center">
              <div class="text-7xl font-bold">٣٥٠</div>
              <div class="text-muted-foreground text-xs uppercase">سعرات حرارية/يوم</div>
            </div>
            <button type="button" class="btn rounded-full" data-variant="outline" data-size="icon-sm" aria-label="زيادة"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14" /><path d="M12 5v14" /></svg></button>
          </div>
        </div>
      </section>
      <footer class="mx-auto w-full max-w-sm">
        <button type="button" class="btn" onclick="this.closest('dialog').close()">إرسال</button>
        <button type="button" class="btn" data-variant="outline" onclick="this.closest('dialog').close()">إلغاء</button>
      </footer>
    </article>
  </dialog>
</div>
</Preview>

## React

```tsx
import { Drawer, useDrawer } from "@a3s-lab/ui/react";

export function GoalDrawer() {
  const drawer = useDrawer();
  return (
    <>
      <button type="button" onClick={() => drawer.call("showModal")}>
        Adjust goal
      </button>
      <Drawer
        ref={drawer.ref}
        data-side="right"
        aria-labelledby="goal-drawer-title"
        aria-describedby="goal-drawer-description"
      >
        <article>
          <header>
            <h2 id="goal-drawer-title">Adjust daily goal</h2>
            <p id="goal-drawer-description">Choose a sustainable task target.</p>
          </header>
          <section>
            <label>Tasks per day <input type="number" min="1" max="20" defaultValue="8" /></label>
          </section>
          <footer>
            <button type="button" onClick={() => drawer.call("close", "saved")}>Save changes</button>
            <button type="button" onClick={() => drawer.call("close")}>Cancel</button>
          </footer>
        </article>
      </Drawer>
    </>
  );
}
```

`useDrawer` exposes `showModal` and `close(returnValue)` through `call()`. The adapter keeps the native dialog lifecycle and adds no framework-only open state.

## Vue

```vue
<script setup>
import { Drawer, useDrawer } from "@a3s-lab/ui/vue";

const drawer = useDrawer();
const componentRef = drawer.componentRef;
</script>

<template>
  <button type="button" @click="drawer.call('showModal')">Adjust goal</button>
  <Drawer
    ref="componentRef"
    data-side="right"
    aria-labelledby="goal-drawer-title"
    aria-describedby="goal-drawer-description"
  >
    <article>
      <header>
        <h2 id="goal-drawer-title">Adjust daily goal</h2>
        <p id="goal-drawer-description">Choose a sustainable task target.</p>
      </header>
      <section>
        <label>Tasks per day <input type="number" min="1" max="20" value="8" /></label>
      </section>
      <footer>
        <button type="button" @click="drawer.call('close', 'saved')">Save changes</button>
        <button type="button" @click="drawer.call('close')">Cancel</button>
      </footer>
    </article>
  </Drawer>
</template>
```

`useDrawer` exposes `showModal` and `close(returnValue)` through `call()`. The adapter keeps the native dialog lifecycle and adds no framework-only open state.
