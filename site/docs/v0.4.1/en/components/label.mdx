# Label

Label gives a form control a persistent, clickable name so its meaning remains clear after placeholder text disappears.

<Preview title="Wrapped control">
  <label class="label gap-2" data-label-primary-demo="en">
    <input type="checkbox" class="input" id="label-terms-en" />
    Accept terms and conditions
  </label>
</Preview>

<Preview title="Association and required">
  <div class="grid gap-4" data-label-association-demo="en">
    <div class="grid gap-2">
      <label class="label" for="label-workspace-en">
        Workspace name
        <span class="ms-auto text-xs font-normal text-muted-foreground" aria-hidden="true">Required</span>
      </label>
      <input class="input" id="label-workspace-en" type="text" name="workspace" required aria-describedby="label-workspace-help-en" />
      <p id="label-workspace-help-en" data-field-description>Shown next to the control, not only as placeholder text.</p>
    </div>
    <div class="grid gap-2" data-disabled="true">
      <label class="label" for="label-locked-en">Billing contact</label>
      <input class="input" id="label-locked-en" type="email" value="owner@example.com" disabled />
      <p data-field-description>Disabled presentation keeps the label readable without fake semantics.</p>
    </div>
  </div>
</Preview>

## Usage

<Steps>
  <Step title="Include CSS">

Import Tailwind and one full A3S UI style bundle.

```css
@import "tailwindcss";
@import "@a3s-lab/ui/a3s.css";
```

Or import only the base CSS, Label component CSS, and one style pack.

```css
@import "tailwindcss";
@import "@a3s-lab/ui/base.css";
@import "@a3s-lab/ui/components/label.css";
@import "@a3s-lab/ui/styles/a3s.css";
```

Using CDN or bundler imports? See the [Installation page](/installation).

  </Step>
  <Step title="Add your label HTML">

Add `class="label"` to a `<label>` element.

```html
<label class="label" for="email">Your email address</label>
```

  </Step>
</Steps>

## Examples

### RTL

Add `dir="rtl"` to the label or an ancestor.

<Preview>
<div class="grid gap-3" dir="rtl">
  <label class="label" for="email-rtl">البريد الإلكتروني</label>
  <input class="input" id="email-rtl" type="email" placeholder="name@example.com" />
</div>
</Preview>

## React

```tsx
import { Label } from "@a3s-lab/ui/react";

export function Example() {
  return <Label htmlFor="email">Your email address</Label>;
}
```

The adapter replaces only the documented semantic root and adds no wrapper; its children keep the HTML contract shown above.

## Vue

```vue
<script setup lang="ts">
import { Label } from "@a3s-lab/ui/vue";
</script>

<template><Label for="email">Your email address</Label></template>
```

The adapter replaces only the documented semantic root and adds no wrapper; its children keep the HTML contract shown above.
